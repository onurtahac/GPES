@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@* MODEL GELECEK *@

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/editForCriteria.css">
    <title>Edit Criteria</title>
</head>
<body>
    <div class="mainEditCriteria">

        @using (Html.BeginForm("EditCriteria", "Criteria", FormMethod.Post))
        {
            /* <div class="form-group">
                <label for="criteriaName">Criteria Name:</label>
                @Html.TextBoxFor(model => model.CriteriaName @*model gelecek*@, new { @id = "criteriaName", @placeholder = "Enter criteria", @class = "form-control", required = "required" })
                    </div>*/
            /*
            <div class="form-group">
            <label for="criteriaPercentage">Percentage (%):</label>
            @Html.TextBoxFor(model => model.Percentage @*model gelecek*@, new { @type = "number", @id = "criteriaPercentage", @placeholder = "Enter percentage", @class = "form-control", min = "0", max = "100", required = "required" })
            </div>*/

            <div class="form-buttons">
                <button type="submit" class="submitButton">Save Criteria</button>
                <button type="button" class="cancelButton">Cancel</button>
            </div>
        }
    </div>
</body>
</html>